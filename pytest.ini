[pytest]
addopts =
    --assert=plain
    --strict-markers
    --strict-config
    -m "not integration"
    --cov=babelarr
    --cov-report=term-missing
    --cov-fail-under=80
markers =
    integration: tests requiring external services or slow operations
